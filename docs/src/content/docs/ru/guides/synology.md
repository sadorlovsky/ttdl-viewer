---
title: Запуск на Synology NAS
description: Container Manager на DSM 7, права на монтирование, которые обычно и кусают, и сборка на ARM-моделях.
---

DSM 7 с установленным **Container Manager**. Архивы уже лежат на NAS — обычно в общей папке вроде
`/volume1/media/tiktok`.

## Через интерфейс Container Manager (проект)

1. Скопируйте этот репозиторий на NAS, например в `/volume1/docker/ttdl-viewer` (File Station или
   `git clone` по SSH).
2. Container Manager → **Проект** → **Создать**. Укажите путь к этой папке; он подхватит
   `docker-compose.yml`.
3. Поправьте строку тома так, чтобы слева была ваша общая папка с архивами:

   ```yaml
   volumes:
     - /volume1/media/tiktok:/archives:ro
   ```

4. Решите, что со строкой порта. `- "127.0.0.1:4174:4174"` оставляет его доступным только самому NAS
   (дальше SSH-туннель или VPN); `- "4174:4174"` выставляет его в локальную сеть без
   аутентификации.
5. Соберите и запустите. Откройте `http://<ip-наса>:4174`, если опубликовали в сеть.

## Или по SSH

```bash
cd /volume1/docker/ttdl-viewer
sudo docker compose up -d --build
```

## Если запустился, но архивов ноль

Почти наверняка монтирование нечитаемо для непривилегированного пользователя `bun` внутри
контейнера. Общие папки Synology часто принадлежат конкретной учётной записи DSM, а не читаемы всем.
Посмотрите числового владельца и скажите compose совпасть с ним:

```bash
ls -ln /volume1/media/tiktok      # например  drwx------ 1026 100
```

```yaml
user: "1026:100"
```

## Архитектура сборки

Большинство моделей Synology — x86-64 и собираются нативно. На ARM-модели (DS220j и похожие)
собирайте на самом NAS, как выше, а не отправляйте туда образ amd64.

Модели с малой памятью могут не вытянуть шаг сборки Vite; если его убивают, соберите образ на
ноутбуке под нужную платформу и загрузите:

```bash
docker buildx build --platform linux/arm64 -t ttdl-viewer . --load
docker save ttdl-viewer | ssh nas 'sudo docker load'
```

## За обратным прокси DSM

Панель управления → Портал входа → Дополнительно → Обратный прокси работает и даёт HTTPS, но своей
аутентификации не добавляет. Сочетайте его с брандмауэром DSM, VPN или аутентифицирующим прокси,
если NAS достижим снаружи дома.

:::caution
Эти инструкции не запускались — они написаны по документированному поведению DSM. См.
[Известные ограничения](/ru/explanation/known-limits/).
:::
