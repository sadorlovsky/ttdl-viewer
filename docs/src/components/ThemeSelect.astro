---
/*
 * No theme selector.
 *
 * The viewer itself is a darkroom — it has no light mode, and DESIGN.md defines no light palette.
 * Offering a toggle that lands on Starlight's own light defaults would show colours this project
 * does not own, so the control is removed rather than styled.
 */
---
